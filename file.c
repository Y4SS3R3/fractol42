#include <stdio.h>

int main ()
{
	int a = 10;
	int b = 6;
	int c = b >> 8;
	printf("%d\n", c);
}
fractal->c.x = map(x,fractal->start.x, fractal->end.x, 599) * fractal->zoom + fractal->zoom_offset.x + fractal->offset.x;
	fractal->c.y = map(y,fractal->start.y, fractal->end.y, 599) * fractal->zoom + fractal->zoom_offset.y + fractal->offset.y;
